# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# uv
# uv.lock is committed for reproducible installs

# autoexplore runs (per-run working dirs are local artifacts, not source)
runs/

# Project-local model / dataset caches (see SKILL.md "Cache 落项目目录")
caches/
